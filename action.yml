name: 'GhostInspector'
description: 'Run GhostInspector tests.'
author: 'UDX'
inputs:
  suite:
    description: 'The ID of the suite to test.'
    required: false
  api_key:
    description: 'Your GI API key.'
    required: false

outputs:
  passing:
    description: 'The ID of the created Release'
runs:
  using: 'node12'
  main: 'lib/index.js'
